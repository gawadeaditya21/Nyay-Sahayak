import React from 'react';

/**
 * TourStep - Dummy wrapper component used for defining steps declaratively if preferred.
 * Currently, GuidedTour accepts a JSON array of steps directly for simplicity,
 * but this file is included for structural completeness as requested.
 * 
 * @component
 * @param {Object} props
 * @param {string} props.target - CSS Selector for the target element
 * @param {string} props.title - Title of the step
 * @param {string} props.content - Description content
 * @returns {null}
 */
export default function TourStep({ target, title, content }) {
  return null;
}
